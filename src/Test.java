import java.io.*;import java.util.ArrayList;import java.util.StringTokenizer;public class Test {    public static void main(String[] args) {        int res = readFileAndReturnResult("lit.in");        System.out.println("Result: " + res);    }    private static int readFileAndReturnResult(String file) {        try {            FileReader fileReader = new FileReader(file);            BufferedReader bufferedReader = new BufferedReader(fileReader);            // word size            int n = Integer.parseInt(bufferedReader.readLine());            // 1st word            StringTokenizer stFirst = new StringTokenizer(bufferedReader.readLine());            ArrayList<Character> word1 = transformStringToArrayList(stFirst.nextToken());            // 2nd word            StringTokenizer stSecond = new StringTokenizer(bufferedReader.readLine());            ArrayList<Character> word2 = transformStringToArrayList(stSecond.nextToken());            if (n != word1.size() || n != word2.size() || (n < 2 || n > 1000000)) {                System.out.println("Wrong input data");            }            return implementaion(word1, word2);        } catch (FileNotFoundException e) {            e.printStackTrace();        } catch (IOException e) {            e.printStackTrace();            System.out.println("Input error");        }        return 0;    }    private static int implementaion(ArrayList<Character> s1, ArrayList<Character> s2) {        int res = 0;        int[] t = new int[s2.size()];        for (int i = 0; i < s1.size(); i++) {            int pos = s1.indexOf(s2.get(i));            t[pos] = 1;            pos = pos + sum(t, pos + 1);            res = res + pos - i;        }        return res;    }    //Used as helper to sum in implementatu    private static int sum(int[] intList, int pos) {        int sum = 0;        for(int i = pos; i < intList.length; i++)            sum += intList[i];        return sum;    }    private static ArrayList<Character> transformStringToArrayList(String str)    {        ArrayList<Character> al = new ArrayList<>();        for(int i = 0; i < str.length(); i++){            al.add(str.charAt(i));        }        return al;    }}